context('timetracker development automation', () => {
  it('correctly calculates total hours', () => {
    cy.visit('http://localhost:3000/');
  });

  it('correctly calculates total rate', () => {
    cy.visit('http://localhost:3000/');
  });

  it('ignores empty hours and rates in calculations', () => {
    cy.visit('http://localhost:3000/');
  });
})
